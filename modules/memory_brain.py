class MemoryBrain:
    def remember(self, key, value):
        return f"🧠 به‌خاطر می‌سپارم: {key} → {value}"

    def recall(self, key):
        return f"📌 یادآوری برای {key}: هنوز حافظه ساده است."
